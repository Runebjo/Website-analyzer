To run site in development:

npm run start:plain

-------------------

To deploy to netlify:

npm run build:plain (something's wrong with postcss on normal build)

netlify deploy (choose "build" folder)